## ✅ Fix applied for the output-routing regression

I restored the legacy output behavior in the non-uClock branch and kept the one-step service logic in the uClock branch, using a compile-time split as requested.

### Verified build evidence
I ran:

/home/interplain/.platformio/penv/bin/platformio run

Result:
- Build status: SUCCESS
- Evidence: "1 succeeded in 00:00:02.380"
- No flash was performed, per your requirement to stop before flashing.

---

## Exact diff before commit

This is the exact patch for the fix in stm32/sequencer_bridge.cpp:

```diff
diff --git a/stm32/sequencer_bridge.cpp b/stm32/sequencer_bridge.cpp
index 34bf252..d1ee06a 100644
--- a/stm32/sequencer_bridge.cpp
+++ b/stm32/sequencer_bridge.cpp
@@ -5,6 +5,7 @@
 #include "platform/fram/fram_layout.h"
 #include "platform/fram/fram_settings.h"
 #include "platform/dac8564/dac8564.h"
+#include "uClock.h"
 #include <cstdio>
 #include <cstring>
 
@@ -46,6 +47,12 @@ static uint32_t s_gate_prev_step = 0xFFFFFFFFu;
 static uint32_t s_gate_prev_loops = 0xFFFFFFFFu;
 static uint8_t s_gate_prev_substep = 0xFFu;
 
+static void Bridge_UClockMusicalCallback(uint32_t tick)
+{
+    (void)tick;
+    g_sequencer.NotifyUClockMusicalCallback();
+}
+
 typedef struct
 {
     uint8_t  magic[4];
@@ -213,6 +220,9 @@ extern "C"
             SaveCvRouterModeSetting();
         }
         g_sequencer.Init();
+#if defined(S12_USE_UCLOCK_MUSICAL_STEP_CLOCK) && S12_USE_UCLOCK_MUSICAL_STEP_CLOCK
+        uClock.setOnSync(uClockClass::PPQN_96, Bridge_UClockMusicalCallback);
+#endif
         /* Keep runtime step state volatile: do not restore song step data on boot. */
 
         Bridge_ApplyZeroOutputCodes();
@@ -624,6 +634,12 @@ extern "C"
     void Bridge_Process(void)
     {
         g_sequencer.Process();
+
+#if defined(S12_USE_UCLOCK_MUSICAL_STEP_CLOCK) && S12_USE_UCLOCK_MUSICAL_STEP_CLOCK
+        /* uClock musical step service writes the snapshot at the top of the loop. */
+#else
+        Bridge_WriteCurrentStepSnapshot();
+#endif
     }
 
     const char* Bridge_GetStepChordDisplayName(uint8_t step_index, char* buf, uint8_t buf_len)
@@ -686,4 +702,24 @@ uint32_t Bridge_GetCompletedLoops(void)
 {
     return g_sequencer.GetCompletedLoops();
 }
+
+uint32_t Bridge_GetUClockMusicalCallbackCount(void)
+{
+    return g_sequencer.GetUClockMusicalCallbackCount();
+}
+
+uint32_t Bridge_GetTickMusicalCount(void)
+{
+    return g_sequencer.GetTickMusicalCount();
+}
+
+uint32_t Bridge_GetPendingStepEnqueueCount(void)
+{
+    return g_sequencer.GetPendingStepEnqueueCount();
+}
+
+uint32_t Bridge_GetServiceOneStepCount(void)
+{
+    return g_sequencer.GetServiceOneStepCount();
+}
+}
```
+
+---
+
+## Behavior after the fix
+
+### Legacy path, flag = 0
+`Bridge_Process()` now does:
+
+```cpp
+g_sequencer.Process();
+
+#if defined(S12_USE_UCLOCK_MUSICAL_STEP_CLOCK) && S12_USE_UCLOCK_MUSICAL_STEP_CLOCK
+    // uClock path
+#else
+    Bridge_WriteCurrentStepSnapshot();
+#endif
+```
+
+This restores the old behavior:
+- `Tick1ms()` advances steps via the millisecond path
+- `Bridge_Process()` writes the current step snapshot to DAC/gates as before
+
+### uClock path, flag = 1
+The existing top-of-loop flow is preserved:
+
+```cpp
+Bridge_ServiceMusicalEvents();
+UI_Input_Poll();
+UI_Sequencer_Update();
+```
+
+and inside `Bridge_ServiceMusicalEvents()`:
+- service exactly one pending uClock step
+- then immediately call `Bridge_WriteCurrentStepSnapshot()`
+
+`Bridge_Process()` no longer redundantly writes the same snapshot on each UI pass in this branch.
+
+---
+
+## Scope kept within requirements
+
+No changes were made to:
+- uClock runtime
+- TIM1
+- J13
+- DAC calibration
+- FRAM
+- ARP timing
+- step timing calculations
+- one-step-per-loop service logic
+
+The fix is limited to the routing bug introduced by the refactor.
